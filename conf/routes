# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                                       Application.Main
GET     /about                                  Application.About
GET     /gmaps                                  Application.Gmaps
GET     /group                                  Application.Group
GET     /group/:groupName                       Application.GroupCurrent
GET     /teacher                                Application.Teacher
GET     /teacher/:teacherName                   Application.TeacherCurrent


GET     /login                                  Application.Login
POST    /login                                  Application.LoginPost
GET     /logout                                 Application.Logout

GET     /admin                                  Admin.Main
GET     /database                               Admin.DataBase
GET     /get_ws_token                           Admin.GetWSToken
GET     /tasks                                  Admin.Tasks

# API
POST    /get_category                           Api.GetCategoryItems
POST    /get_category_list                      Api.GetCategoryList
POST    /get_faculty_groups                     Api.GetFacultyGroups
POST    /get_faculty_schedule                   Api.GetFacultySchedule
POST    /get_faculty_tasks                      Api.GetFacultyTasks
POST    /update_item                            Api.UpdateItem

Application
WS      /ws/socket                              Admin.SocketConn

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
